{
  "coverage_report_locations": [
  ],
  // A list of locations which to remove from the beginning of a coverage report's filename
  // An example is:
  //    I run my coverage reports in /home/jeffrand/tested_code, which may not show up in the coverage
  //    report's file names, but will be in my path as its opened in sublime, so I want to strip that location
  //    from the file's name
  "strip_locations": [
  ]
}
